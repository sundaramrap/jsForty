console.log("...connected");

const form = document.createElement("form");

// Creating form label
const label1 = document.createElement("label");
label1.innerText = "Select Your Specifications";
form.appendChild(label1);
form.appendChild(document.createElement("br"));
form.appendChild(document.createElement("br"));

// Shared radio group name
const radioName = "userSpec";

// Helper to create radio + label
function createRadio(id, value, text) {
  const radio = document.createElement("input");
  radio.type = "radio";
  radio.id = id;
  radio.value = value;
  radio.name = radioName;

  const label = document.createElement("label");
  label.setAttribute("for", id);
  label.innerText = text;

  form.appendChild(label);
  form.appendChild(radio);
  form.appendChild(document.createElement("br"));
}

// Creating all radio buttons
createRadio("uName", "name", "Name");
createRadio("uEmail", "email", "Email");
createRadio("uNumber", "number", "Contact");
createRadio("uCity", "city", "City");

// Create and append the save button
const saveBtn = document.createElement("button");
saveBtn.id = "save";
saveBtn.innerText = "SAVE";
saveBtn.type = "submit"; // Optional
form.appendChild(saveBtn);

// Append form to DOM
const userForm = document.getElementById("userForm");
userForm.appendChild(form);
