console.log(`...dom Connected`);
//make checkBox and check the box according to the required criteria
const form = document.createElement("form");
const container = document.getElementById("userForm");
container.appendChild(form);

//helper function for checkBox and label

function checkBoxSamp(id, value, text) {
  const checkbox = document.createElement("input");
  checkbox.input = "checkbox";
  checkbox.id = id;
  //checkBox InputValue
}
//complementinh
function checkInput() {
  const checkInput = document.createElement("This ");
}
